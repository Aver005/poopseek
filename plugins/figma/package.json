{
  "name": "poopseek-figma-plugin",
  "private": true,
  "scripts": {
    "build": "bun run build.ts",
    "watch": "bun run --watch build.ts",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.108.0",
    "esbuild": "^0.28.0"
  }
}
